package demo13;

class Solution {
    public int romanToInt(String s) {
        int length = s.length();
        char tmp;
        for (int i = 0; i < length; i++) {
            tmp = s.charAt(i);

        }

        return 0;
    }
}